const roleNos: number[]=[]
const names: string[]=[]
const grades: Array<number>=[]

type subjectScore={
    subject:string,
    marks: number
}

const allMarks: subjectScore[]=[]

roleNos.push(2,23,22);
allMarks.push({subject:"Maths", marks:99})